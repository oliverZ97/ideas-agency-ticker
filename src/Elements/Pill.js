import React from 'react';

const Pill = (props) => {

    return (
        <button className="pill pill__secondary">
            <span className="pill__icon">
                <img src="../"></img>
            </span>
        </button>
    );
}

export default Pill;